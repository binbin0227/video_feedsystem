# FrameFlow 前端

Go Hertz 短视频后端的项目展示前端，使用 Vue 3、Vite、JavaScript、Vue Router、Axios 和普通 CSS。

## 本地启动

```bash
npm install
npm run dev
```

开发地址默认为 `http://127.0.0.1:5189`，后端地址默认是 `http://localhost:20000`。

如需覆盖后端地址，复制 `.env.example` 为 `.env.local` 并修改 `VITE_API_BASE_URL`。
